{
  "name": "hemicycle-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Narrow allowlisted proxy for Congress.gov + Senate.gov (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.90.0"
  }
}
